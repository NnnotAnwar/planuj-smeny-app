# --- Supabase (client) ---
# Project URL, e.g. https://<project-ref>.supabase.co
VITE_SUPABASE_URL=

# Publishable (anon) key from Supabase → Project Settings → API.
# This is a public client key — never put the service_role key here.
VITE_SUPABASE_PUBLISHABLE_KEY=
